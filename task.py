from class1 import NewHello  # Импортируем класс NewHello из файла class1

# Создаем объект класса NewHello
a = NewHello(NewHello)

# Применяем функцию tprint из модуля art к объекту a
from art import tprint
tprint(str(a))  # Используем str(a), чтобы получить строковое представление объекта a
